# Example local environment configuration. Do not commit real secrets.
GROQ_API_KEY=
APP_ENV=development
APP_HOST=127.0.0.1
APP_PORT=8000
STREAMLIT_PORT=8501
# Tavily API key for web search fallback (optional — leave empty to disable web search)
# Get your free key at https://tavily.com (1000 searches/month free tier)
TAVILY_API_KEY=
